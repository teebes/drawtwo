# Frontend Environment Variables Template
# Copy this to .env.local (gitignored) if you need to override defaults
# Note: Vite requires variable names to start with VITE_ to be exposed to the app

# API Base URL (optional - auto-detects based on hostname if not set)
# VITE_API_BASE_URL=http://localhost:8002/api

# Base URL for WebSocket connections (optional - auto-detects if not set)
# VITE_BASE_URL=http://localhost:8002

# Note: Card asset URLs are provided by the backend API
# No frontend configuration needed for Cloudflare R2 integration
